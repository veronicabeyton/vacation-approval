# vacation-approval-system.js
This code demonstrates how to send an interactive HTML email with approval links, handle the manager's click via an API web app endpoint (doGet), update the AppSheet database, and return a clean success webpage.
